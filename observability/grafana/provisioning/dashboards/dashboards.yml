apiVersion: 1

providers:
  - name: Library Service
    orgId: 1
    folder: Library Service
    folderUid: library-service
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
